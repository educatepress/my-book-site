/**
 * Slack報告の一括スイッチ（2026-08-27 取締役指示「監視は続けてよいがSlack報告は終了。見ないから」）。
 *
 * 巡回・監視・生成そのものは従来どおり動かし、その結果をSlackへ投げる処理だけを止める。
 * 送信側の各関数はこの定数を見て早期returnするので、再開したくなったら **この1行を true に戻す**
 * だけでよい（呼び出し側・メッセージ本文はいずれも無改変で残してある）。
 *
 * 注意: これを切ると障害通知も止まる。失敗は Vercel Logs / GitHub Actions のログに残るだけになる。
 */
export const SLACK_REPORTS_ENABLED = false;
